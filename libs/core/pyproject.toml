[project]
name = "gnovi-core"
version = "0.1.0"
description = "Gnovi shared domain logic, types, and utilities"
requires-python = ">=3.13"
license = "AGPL-3.0-only"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gnovi"]
